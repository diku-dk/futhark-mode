language: minimal

sudo: false

dist: trusty

git:
  submodules: false

addons:
  apt:
    sources:
      - sourceline: ppa:kelleyk/emacs
    packages:
    - emacs26

script:
 - ./tools/test-indentation
